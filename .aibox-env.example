# aibox Configuration
# Copy this file to .aibox-env.ACCOUNT_NAME for each account you want to use
# Example: .aibox-env.work, .aibox-env.personal

# Account identifier (used for volume naming and container naming)
AI_ACCOUNT=default

# AI CLI Type (claude, codex, or gemini)
AI_CLI=claude

# Container User (avoids conflict with system USER variable)
CONTAINER_USER=ai

# Git Configuration (for commits made by AI tools)
GIT_AUTHOR_NAME=Name
GIT_AUTHOR_EMAIL=email@example.com
GIT_COMMITTER_NAME=Name
GIT_COMMITTER_EMAIL=email@example.com

# SSH Configuration
# Path to SSH keys (default: ~/.ssh)
# SSH_KEY_PATH=~/.ssh
# SSH_KEY_FILE=id_rsa_work

# Environment file
ENV_FILE=.env.local

# Node.js Environment
NODE_ENV=development

# Timezone (optional)
# TZ=America/New_York

# github-cli authentication (optional)
# GH_TOKEN=